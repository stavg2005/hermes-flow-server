[server]
address = "0.0.0.0" # Listen on all interfaces in container
port = 5000
threads = 4

[s3]
host = "minio" # Resolves via Docker bridge network
port = "9000"
bucket = "audio-files"
access_key = "minioadmin"
secret_key = "minioadmin123"
region = "us-east-1"
service ="s3"

[janus]
address = "janus" # Resolves via Docker bridge network
port_start = "10000"
port_end ="10200"
