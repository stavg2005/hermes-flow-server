---
Checks: >
  -*,
  bugprone-*,
  performance-*,
  readability-*,
  -bugprone-unchecked-optional-access,
  -readability-identifier-length

# This regex is the fix. It now only matches files that are
# inside your project's "hermes-flow-server/src" or
# "hermes-flow-server/include" directories.
HeaderFilterRegex: '.*/hermes-flow-server/(src|include)/.*'

CheckOptions:
  - key: readability-identifier-naming.VariableCase
    value: camelCase
...
